export interface UserData {
    login: string;
    id: string;
    html_url: string;
    avatar_url:string;
}

export interface GoogleData {
    email:string;
    picture: string;
    name: string;
    sub: string;
}